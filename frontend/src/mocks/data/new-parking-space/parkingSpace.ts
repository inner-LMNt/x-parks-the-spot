// src/mocks/data/new-parking-space/parkingSpace.ts

import { ParkingSpace } from '@/types/type';

/**
 * Parameters for Mock Data Generation
 */
export const settings = {
    count: 15, // Number of items to generate
};

/**
 * Export the type for the generator script
 */
export type MockDataType = ParkingSpace;
